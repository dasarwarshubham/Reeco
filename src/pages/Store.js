import React from "react"

const Store = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '88vh',
      }}
    >
      <h2>Store Component</h2>
    </div>
  );
};

export default Store;
